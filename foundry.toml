[profile.default]
solc = "0.8.28"  # No caret (^) to avoid ambiguity
src = 'contracts'
out = 'out'
libs = ['node_modules', 'lib']
test = 'test'
cache_path  = 'cache_forge'
gas_reports = ["SupplyChain", "SupplyChainFactory"]